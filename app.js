// Get the DOM elements
const generatePasswords = document.getElementById("generate-passwords");

function createPasswords() {

}

// Listen for clicks on the button;
generatePasswords.addEventListener("click", createPasswords);